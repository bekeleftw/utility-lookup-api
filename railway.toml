[build]
buildCommand = "pip install -r requirements.txt && PLAYWRIGHT_BROWSERS_PATH=/app/.playwright playwright install chromium && playwright install-deps"

[deploy]
startCommand = "gunicorn api:app --bind 0.0.0.0:$PORT --timeout 120"

[variables]
PLAYWRIGHT_BROWSERS_PATH = "/app/.playwright"